# macOS
.DS_Store

# Dependencies
node_modules/

# Build output is vendored (committed) so the package works from a git install.
# Rebuild with `npm run build` before committing changes to src/.

# Lockfiles (using pnpm)
package-lock.json

# Reference upstream Python project (kept locally only)
parakeet-mlx/

# Working notes
autonomous-progress.md
autonomous-prompt.md
autonomous-review.md
status.md
